#!/bin/sh

#This script only activates when user selected to encrypt devices,
#so in this last part is needed to get inside with chroot /mnt again and for
# that it's this script for, to update mkinitcpio and grub with
# the latestest changes made.
